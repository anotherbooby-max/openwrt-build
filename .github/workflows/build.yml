name: Build OpenWrt x86_64 WiFi5100


on:
  workflow_dispatch:


env:

  SOURCE_REPO: https://github.com/coolsnowwolf/lede.git

  SOURCE_BRANCH: master



jobs:

  build:

    runs-on: ubuntu-22.04


    steps:


    - name: Checkout

      uses: actions/checkout@v4



    - name: Install dependencies

      run: |

        sudo apt update

        sudo apt install -y \
        build-essential \
        clang \
        flex \
        bison \
        g++ \
        gawk \
        gcc-multilib \
        gettext \
        git \
        libncurses5-dev \
        libssl-dev \
        python3-distutils \
        rsync \
        unzip \
        zlib1g-dev \
        file



    - name: Clone LEDE

      run: |

        git clone \
        --depth=1 \
        --branch ${SOURCE_BRANCH} \
        ${SOURCE_REPO} \
        lede


        cd lede


        echo "===== SOURCE ====="

        git branch

        git log -1 --oneline



    - name: Load x86 config

      run: |

        cp configs/x86_64.config lede/.config



    - name: Update feeds

      run: |

        cd lede

        ./scripts/feeds update -a

        ./scripts/feeds install -a



    - name: Defconfig

      run: |

        cd lede

        make defconfig



    - name: Download

      run: |

        cd lede

        make download -j8



    - name: Compile

      run: |

        cd lede

        make -j$(nproc) V=s



    - name: Firmware

      run: |

        ls -lh lede/bin/targets/x86/64



    - name: Upload

      uses: actions/upload-artifact@v4

      with:

        name: openwrt-x86-64-wifi5100

        path:

          lede/bin/targets/x86/64/*
